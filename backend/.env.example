# Configuration is managed via the root .env file.
# See /home/ubuntu/Projects/Sift/.env.example for all environment variables.
#
# For local development (running backend directly without Docker):
# Copy root .env.example to .env in the project root and fill in values.
# The backend will automatically find it at ../.env
